整理日志

## 2026-07-10｜第一轮整理

### 边界

- 目标文件夹：`/Users/toumingmo/Desktop/公司资料_副本`
- 用户已确认执行整理。
- 不删除、不覆盖、不改写原文件。
- 不修改原文件名。
- 疑似重复或无法确认的内容放入待确认目录。

### 盘点结果

- 原文件夹共 25 个文件，总大小约 2.4 MB。
- 其中业务文件 22 个，macOS 系统元数据文件 3 个。
- 文件类型：12 个 Excel、4 个 Word、2 张图片、1 个 PDF、1 个 TXT、2 个 ZIP 和 3 个 `.DS_Store`。

### 重复比对

- 对 `TORGMOLL` 和 `TORGMOLL_副本` 中的六个业务文件计算 SHA-256。
- 六组哈希均完全一致，确认两套业务文件内容相同。
- 重复副本保留并移入待确认目录，没有删除。

### 执行内容

- 创建 `01_公司与合作方`、`02_报价与业务数据`、`03_线路与市场资料`、`04_演讲与品牌素材` 和 `90_待确认/疑似重复`。
- 按用途移动十个根目录业务文件和两个 TORGMOLL 目录。
- 生成 `README.md`、`文件索引.md`、`待确认清单.md` 和 `整理日志.md`。

### 结果

- 原业务文件全部保留。
- 没有删除、覆盖或改名。
- 后续应先处理 `待确认清单.md` 中的问题，再进行第二轮调整。

## 2026-07-10｜删除已确认的重复副本

### 用户指令

- 用户明确要求直接删除 `TORGMOLL_副本`。
- 该指令仅覆盖本次删除对象的默认禁止删除规则，不扩展到其他文件。

### 执行前确认

- 删除对象：`90_待确认/疑似重复/TORGMOLL_副本/`。
- 其中六个业务文件与主目录 `01_公司与合作方/TORGMOLL/` 的对应文件 SHA-256 完全一致。
- 主目录存在且保留不变。

### 执行结果

- 已删除 `TORGMOLL_副本` 目录及其 7 个文件（包含 6 个重复业务文件和 1 个 `.DS_Store`）。
- 未删除、覆盖或修改其他业务文件。
- 已同步更新 `README.md`、`文件索引.md`、`待确认清单.md` 和本日志。
