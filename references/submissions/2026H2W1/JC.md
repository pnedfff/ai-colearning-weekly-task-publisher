# 2026H2W1 打卡｜JC

> **提交人（群昵称或真实姓名）：JC**
> **GitHub 账号：szcd666**
> 任务：用 Codex 安全整理一个混乱文件夹
> 提交路径：`references/submissions/2026H2W1/JC.md`

---

## 打卡正文

**1. 我整理的文件夹类型：**
用户的 Windows 桌面——一个真实但不重要的混乱文件夹（应用快捷方式、网页快捷方式、零散文本、软件安装包、图片素材、项目仓库目录混在一起）。

**2. 原始文件数量：**
27 个顶层项（13 个 `.lnk`、4 个 `.url`、4 个文件夹、1 个 `.exe`、1 个 `.zip`、3 个 `.txt`、1 个系统隐藏文件 `desktop.ini`）。

**3. Codex 盘点出的主要文件类型：**
- 应用快捷方式 `.lnk` ×13（游戏 / 工具 / 通讯类）
- 网页快捷方式 `.url` ×4
- 文件夹 ×4（其中两个是同一作业仓库的 git 工作副本与源码解压，一个是 AI 关键词配图，一个是个人笔记）
- 可执行文件 `.exe` ×1（本地大模型运行器，约 96 MB）
- 压缩包 `.zip` ×1（本周作业）
- 文本文件 `.txt` ×3（内容极小，疑似临时记录）
- 系统隐藏文件 ×1（不动）

**4. Codex 建议的分类结构：**
```
整理副本/
├── AGENTS.md            # 整理规则与边界
├── 00-整理说明/         # 过程文档（盘点/方案/待确认/日志/说明）
├── 快捷方式/             # 17 个 .lnk + .url
├── 软件/                 # 可执行程序
├── 笔记与文档/           # 作业压缩包 + 散落文本 + 个人笔记/计划
├── 图片素材/             # 原 AI关键词 25 张配图
└── 项目/                 # 两个 ai-colearning 仓库目录
```

**5. 我确认后执行了哪些整理：**
- 先在**副本**上操作，原桌面始终未动（副本即备份）。
- 建立 5 个分类文件夹。
- 分批移动归并：17 个快捷方式 → `快捷方式/`；`.exe` → `软件/`；`作业.zip` + 3 个散落 `.txt` + 原"新建文件夹"内 5 项笔记 → `笔记与文档/`；25 张 AI关键词 PNG → `图片素材/`；两个仓库目录 → `项目/`。
- 清空了被合并后的空文件夹。
- **全程未删除任何文件**。

**6. Codex 生成了哪些说明文档：**
`AGENTS.md`（规则）、`盘点与索引.md`（只读盘点 + 文件索引）、`整理方案.md`（执行前方案）、`待确认清单.md`、`整理日志.md`（执行时间线与移动明细）、`README.md`（整理说明）。

**7. 待确认清单里有哪些典型问题：**
- 同名"新建文本文档"冲突：根目录与"新建文件夹"内各有一个同名 `.txt`，内容不同。
- 两个同源仓库目录冗余：git 工作副本 vs 源码解压，是否删冗余解压目录？
- `作业.zip` 是否需要解压查看？
- 几个极小文本文件是否仍有保留价值？
- 17 个快捷方式是否全部移入子文件夹（还是顶层留几个常用的）？
- 图片素材是否需统一命名规则？

**8. 我做的二次调整是什么：**
首轮整理后核对时发现，并行移动中发生了同名 `新建文本文档.txt` 的**覆盖**（任务明令禁止覆盖）。二次调整：从**始终未动的原桌面**取回被覆盖的根版本，以区分名 `新建文本文档-桌面根.txt` 保留，确保零覆盖、零丢失；并在 `项目/` 加 `说明.txt` 区分 git 工作副本与解压副本。

**9. 这次我学到的一个安全使用 Codex 的经验：**
"先复制副本、首轮只读盘点、出方案确认后再执行、不删不覆盖、执行留日志"这套流程不是流程负担，而是真能救命——本次同名覆盖就是靠"副本 + 原文件未动"才在不丢数据的前提下找回来的。以后把本地任务交给 AI，我会坚持：操作对象必须是副本、同名文件用区分名保留而非覆盖、每一步留文档可查。

---

## 安全自查（提交前已移除敏感信息）
- 未包含任何密码、密钥、证书、合同、身份证件、未公开客户资料。
- 未包含本机绝对路径（仅以"原桌面 / 副本"等泛称描述）。
- 未包含邮箱等个人联系方式（仅保留任务要求的提交人 JC 与 GitHub 账号 szcd666）。
