2026H2W3 打卡：

提交人（群昵称或真实姓名，二选一）：佟掌柜
GitHub 账号：klaustong

1. 我使用的数据类型和文件数量：
   - 课程测试数据：1个 Markdown 文件（YAI 导出的对话记录，约3900行）
   - 内容是一堂双三角画布的完整深度对话：从法律服务工作流初步咨询到脱口秀第二曲线
   - 文件头部包含导出元数据（时间戳、消息总数）

2. 我如何准备安全副本和完成脱敏：
   - 原始文件位于 Desktop，通过 Python `shutil.copy2` 创建副本到 WorkBuddy workspace
   - 原文件全程只读，修复版另存为新文件 `法律服务工作流对话记录_修复版.md`
   - 对话内容本身是AI培训方法论的讨论，不涉及客户隐私数据，天然脱敏

3. 只读扫描发现了哪些待核实问题：
   - 导出时间（UTC）与消息时间（CST）相差8小时，未标注时区
   - 元数据声称"共94条消息"，需校验
   - 章节间空行数量不统一（1行 vs 2-3行）
   - 表格末尾管道符部分缺失
   - v2.0备忘录中"同 v1.0"引用缺少交叉定位
   - 表头分隔行后存在空行（可能影响部分渲染器）

4. Codex 向我确认的 5 个关键口径是什么：
   - 口径1：时区以哪个为准？→ 保留原有，增加标注
   - 口径2：消息计数是否校验？→ 用 grep 校验，确认94条准确
   - 口径3：空行统一为几行？→ 统一为1行
   - 口径4：表格末尾管道符是否统一？→ 成本高收益低，暂不修复
   - 口径5：emoji是否保留？→ 保留

5. 我最终确认了哪些检查标准：
   - 8项检查标准（见 `自检清单.md`），含必须通过2项、建议通过4项、可选2项
   - 必须通过：元数据完整性、消息计数准确性
   - 建议通过：时间戳一致性、标题层级规范、表格格式统一、内容完整性
   - 可选：空行分隔规则、特殊字符编码

6. 问题清单中有哪些真问题和合理例外：
   - **真问题（5个）**：时区标注缺失、空行不统一、表格管道符不一致、v2.0引用占位、表头后空行
   - **合理例外（3个）**：消息计数94条经校验准确、标题省略时区是设计意图、v2.0简写是节省篇幅
   - **人工纠正1次**：表格管道符从"建议修复"降级为"已知但不修复"——1000+行改动成本过高

7. 我生成了哪些候选修复版和日志文件：
   - `自检清单.md`（8项检查标准）
   - `问题清单.md`（5个问题 + 3个合理例外）
   - `法律服务工作流对话记录_修复版.md`（4类修复）
   - `检查日志.md`（修复记录 + 原版对照 + 3个抽查点）

8. 我人工抽查了哪 3 个修复点：
   - 抽查点1：Fix 1 导出时间标注 — UTC 14:59 + 8h = CST 22:59 与末条消息吻合 ✅
   - 抽查点2：Fix 2 v2.0交叉引用 — 跳转第1643行确认v1.0场景章节存在 ✅
   - 抽查点3：Fix 4 空行归一化 — 随机抽3个标题前均为恰好1空行 ✅

9. 我做的一次人工纠正是什么：
   - 表格末尾管道符统一添加：起初列为"建议修复"，执行时发现涉及1000+行改动，且GitHub等主流渲染器均正确渲染。最终判定：降级为"已知但暂不修复"，在检查日志中记录理由。

10. 这次我学到的一条数据核查经验：
    - **区分"修复"和"标注"**：时区不一致不等于数据错误，只需增加标注让读者明白即可。不是所有"不一致"都需要用修改数据来解决——有时一行注释比批量替换更有价值。同时，"修不修"的决策也要看成本：1000行格式统一如果渲染器已经兼容，投入产出比并不划算。

脱敏说明：只描述数据类型、检查过程和问题类别，不粘贴真实数据、客户资料、联系方式、账号、订单号、本机绝对路径或其他敏感内容。
